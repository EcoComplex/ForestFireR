% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{generate_landscape_layers}
\alias{generate_landscape_layers}
\title{Build a landscape from several placement layers}
\usage{
generate_landscape_layers(L = 100, fill_state, layers, seed = NULL)
}
\arguments{
\item{L}{Grid side length.}

\item{fill_state}{The state (\link{FF_STATE}) the whole grid starts as,
before any layer is placed.}

\item{layers}{A list of layers, each itself a list with elements
\code{background}, \code{pattern} (states from \link{FF_STATE}), \code{density} (fraction
of \code{L*L}) and \code{p} (heterogeneity, as in \code{\link[=generate_landscape]{generate_landscape()}}).}

\item{seed}{Integer RNG seed, or \code{NULL} for a fresh, non-reproducible seed.}
}
\value{
An \code{L}x\code{L} integer matrix using \link{FF_STATE}'s codes.
}
\description{
Generalizes \code{\link[=generate_landscape]{generate_landscape()}} to construct initial conditions it
can't express on its own: more than one pattern placed independently
over a shared background, or a small pattern placed over an
already-uniform domain. The whole grid is first filled with
\code{fill_state}; then each layer in \code{layers}, in order, grows its \code{pattern}
state to \code{density * L*L} cells (a fraction of the WHOLE grid, same
convention as \code{\link[=generate_landscape]{generate_landscape()}}'s \code{density2}) with heterogeneity
\code{p} (same meaning as there), drawing candidate cells only from whatever
\code{background} state is still left after the layers before it. If a
layer's target can't be reached because its background ran out (e.g.
the density budget across layers doesn't leave enough), the engine
prints a one-time notice and stops that layer early rather than
hanging -- check the printed cell counts against what you asked for if
you see this.
}
\details{
Used to build the initial conditions for Figs. 8-9 (5\% native + 5\%
invader, placed independently, over an all-\code{empty_postfire} domain) and
Figs. 10-11 (1\% fire placed over an all-\code{invader} domain) -- see
\verb{Source/R_reproduce_figures/} in the manuscript repository for the full
reproduction scripts.
}
\examples{
\dontrun{
# 5\% native + 5\% invader over a post-fire-empty domain (Figs. 8-9)
g <- generate_landscape_layers(L = 100, fill_state = FF_STATE["empty_postfire"],
  layers = list(
    list(background = FF_STATE["empty_postfire"], pattern = FF_STATE["native"],
         density = 0.05, p = 0.8),
    list(background = FF_STATE["empty_postfire"], pattern = FF_STATE["invader"],
         density = 0.05, p = 0.8)
  ), seed = 1)

# 1\% fire over an all-invader domain (Figs. 10-11)
g2 <- generate_landscape_layers(L = 100, fill_state = FF_STATE["invader"],
  layers = list(
    list(background = FF_STATE["invader"], pattern = FF_STATE["fire"],
         density = 0.01, p = 0.8)
  ), seed = 1)
}
}
